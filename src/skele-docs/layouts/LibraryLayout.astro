---
import { z } from "zod/v4";
import "../../styles/skele.pcss";
import "../styles/skele-docs.pcss";

const libraryLayoutSchema = z.object({
  frontmatter: z
    .object({
      title: z.string().optional(),
      content_blocks: z.array(z.any()).optional(),
    })
    .optional(),
});

import Container from "../../components/layouts/container/container.astro";
import NavLayout from "./NavLayout.astro";
const props = libraryLayoutSchema.parse(Astro.props);
const pageTitle = props.frontmatter?.title || "Skele Library";
---

<!doctype html>
<html lang="en" data-theme="default">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <slot name="head" />
  </head>
  <body class="animations-disabled skele-docs">
    <Container
      maxContentWidth="2xl"
      paddingHorizontal="md"
      paddingVertical="sm"
      class="library-container"
    >
      <NavLayout />
      <main class="main">
        <slot />
      </main>
    </Container>
    <script>
      setTimeout(() => {
        document.body.classList.remove("animations-disabled");
      }, 1000);
    </script>
  </body>
</html>
