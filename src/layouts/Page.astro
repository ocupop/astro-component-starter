---
import Components from "@components/utils/renderBlock.astro";
import Layout from "@layouts/BaseLayout.astro";

type Props = {
  frontmatter: {
    title: string;
    description?: string;
    keywords?: string[];
    image?: string;
    canonical?: string;
    pageSections: any[];
  };
};

const props = Astro.props.frontmatter;
---

<Layout
  title={props.title}
  description={props.description}
  keywords={props.keywords}
  image={props.image}
  canonical={props.canonical}
>
  <Components contentSections={props.pageSections} propName="pageSections" />
</Layout>
